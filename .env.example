# ---------------- Core Environment ----------------
ENVIRONMENT=development
DATABASE_URL=resume_analyzer.db

# ---------------- OpenAI Settings -----------------
# Requests per minute per API key (default: 480)
OPENAI_RPM_PER_KEY=480

# Fail fast on hitting RPM limit (1) or block until free (0, default)
OPENAI_RPM_FAIL_FAST=0

# Max total blocking delay in ms when throttled (default: 3600000 = 1h)
OPENAI_RPM_MAX_DELAY_MS=3600000

# Cap concurrent in-flight OpenAI calls per API key (0 disables, default: 20)
OPENAI_MAX_CONCURRENCY_PER_KEY=20

# Optional Redis URL for cluster-wide RPM (leave empty for local in-memory limiter)
OPENAI_REDIS_URL=

# ---------------- Optional Overrides ----------------
# Only override if you want to change defaults in config.py
# MAX_JOBS_PER_API_KEY=20
# MAX_JOBS_PER_USER=1
# JOB_CLEANUP_MINUTES=60
